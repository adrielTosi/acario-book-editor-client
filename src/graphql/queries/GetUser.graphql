query GetUser($username: String!) {
  getUser(username: $username) {
    id
    name
    username
    avatarType
    avatarSeed
    bio
    numberOfFollowing
    numberOfFollowers
    _count {
      chapters
    }
    chapters {
      ...Chapter
    }
  }
}
