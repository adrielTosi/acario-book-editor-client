query getChaptersFromUser($username: String!, $take: Float!, $offset: Float!) {
  getChaptersFromUser(username: $username, take: $take, offset: $offset) {
    chapters {
      ...Chapter
    }
    hasMore
  }
}
